{
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "features": {
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y curl build-essential libreadline-dev zlib1g-dev flex bison libxml2-dev libxslt-dev libssl-dev libxml2-utils xsltproc ccache meson libipc-run-perl",
  "runArgs": [ "--cap-add=SYS_PTRACE", "--security-opt", "seccomp=unconfined" ]
}
